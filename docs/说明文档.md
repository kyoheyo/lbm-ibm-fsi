# LBM-IBM-FSI 项目说明文档

## 一、项目概述

本项目实现了一个高性能的**格子 Boltzmann 法（LBM）+ 浸入边界法（IBM）+ 流固耦合（FSI）**仿真框架，采用多语言混合架构，在性能、安全性和可扩展性之间取得平衡。

---

## 二、项目结构

```
lbm-ibm-fsi/
├── CMakeLists.txt               # 顶层 CMake（C++ 核心）
├── Cargo.toml                   # Rust 工作空间
├── README.md                    # 英文说明文档
├── docs/
│   └── 说明文档.md              # 本中文说明文档
│
├── core/                        # C++ 计算核心
│   ├── CMakeLists.txt
│   ├── include/
│   │   ├── lbm/
│   │   │   ├── lattice.hpp      # 格子模型（D2Q9 / D3Q19 / D3Q27）
│   │   │   ├── solver.hpp       # BGK/MRT 碰撞 + 迁移
│   │   │   └── boundary.hpp     # 边界条件（反弹、ZHE 等）
│   │   ├── ibm/
│   │   │   ├── marker.hpp       # 拉格朗日标记点
│   │   │   └── interpolation.hpp # Delta 函数展布/插值
│   │   ├── fsi/
│   │   │   ├── structure.hpp    # 柔性梁/壳有限元
│   │   │   └── coupling.hpp     # 分区式 FSI 耦合
│   │   └── plugins/             # ★ 插件扩展接口（纯头文件）
│   │       ├── boundary_plugin.hpp   # IBoundaryPlugin — 自定义边界条件
│   │       ├── mesh_plugin.hpp       # IMeshPlugin — 自适应/非均匀网格
│   │       ├── motion_plugin.hpp     # IMotionPlugin — 运动网格/固体运动
│   │       ├── flexible_plugin.hpp   # IFlexibleSolverPlugin — 柔性结构求解器
│   │       └── plugin_registry.hpp   # PluginRegistry — 全局插件注册中心
│   └── src/
│       ├── lbm/                 # LBM 碰撞、迁移实现
│       ├── ibm/                 # IBM 力展布与插值
│       ├── fsi/                 # FSI 结构求解
│       ├── capi/                # C ABI 桥接层（供 Rust FFI 调用）
│       └── plugins/             # ★ 插件 C ABI 适配器
│           └── plugin_registry.cpp  # C 函数指针 → C++ 虚函数适配器
│
├── bindings/                    # Rust crate — C ABI 封装 + 安全 Rust 包装
│   ├── Cargo.toml
│   ├── build.rs                 # 编译期调用 cmake 构建 C++ 核心
│   └── src/lib.rs               # ★ 含 PluginCallbacks + register_plugins()
│
├── orchestrator/                # Rust crate — CLI 驱动 + 仿真主循环
│   ├── Cargo.toml
│   └── src/
│       ├── main.rs              # 主控流程：预处理 → 仿真 → 后处理
│       ├── config.rs            # TOML 配置文件解析（含 ★ PluginsConfig）
│       ├── output.rs            # 原生 Rust 输出：NPZ 快照 + CSV 监控日志
│       └── python_bridge.rs     # Python FFI 桥接（pyo3，可选特性）
│
├── python/                      # Python 预/后处理包
│   ├── pyproject.toml
│   ├── lbm_pre/                 # 预处理包
│   │   ├── __init__.py
│   │   ├── mesh.py              # gmsh 网格生成
│   │   ├── geometry.py          # IBM 拉格朗日标记生成
│   │   ├── config_gen.py        # 从 Python 生成求解器 TOML 配置
│   │   └── bridge.py            # FFI 友好接口（供 pyo3 调用）
│   ├── lbm_post/                # 后处理包
│   │   ├── __init__.py
│   │   ├── vtk_reader.py        # 读取 .npz / .vtu 仿真输出
│   │   ├── plot.py              # 等值线、流线、涡量、梁变形图
│   │   ├── analysis.py          # 阻力/升力、Q准则、误差范数、收敛性
│   │   └── bridge.py            # FFI 友好接口（供 pyo3 调用）
│   ├── tests/                   # pytest 测试套件
│   └── examples/
│       └── lid_driven_cavity.py # 端到端预/后处理示例
│
└── configs/                     # 示例 TOML 配置文件
    └── lid_driven_cavity.toml
```

---

## 三、各层职责说明

| 层级 | 语言 | 主要职责 |
|------|------|----------|
| 计算核心 | **C++17** | LBM 碰撞/迁移、IBM 耦合、FSI 结构求解；MPI + OpenMP 并行 |
| Rust 绑定 | **Rust** | 通过 C ABI 包装 C++ 库，提供内存安全的 `LbmGrid`/`LbmSolver` 类型 |
| 主控/驱动 | **Rust** | 配置加载、仿真主循环、NPZ 快照输出、CSV 监控、Python 调用 |
| 预/后处理 | **Python 3.11+** | 网格生成、配置生成、结果可视化、定量分析 |
| 构建系统 | **CMake + Cargo** | C++ 静态库编译；Rust 工作空间管理 |

---

## 四、实现思路

### 4.1 设计原则

本框架遵循以下核心设计原则：

1. **各层只做最擅长的事**：计算密集型内核用 C++ 实现；安全驱动层用 Rust；科学计算与可视化用 Python。
2. **零拷贝或最小拷贝数据传输**：
   - Rust ↔ C++ 之间通过裸指针（C ABI）直接操作同一块堆内存；
   - Rust → Python 之间通过 pyo3 以 Python `list` 传递数组，避免写入中间文件。
3. **输出频率与开销匹配**：
   - 高频（每步或每数百步）：Rust 原生写出 NPZ 快照或 CSV 行，开销极低；
   - 低频（每数千步）：调用 Python 生成高质量云图，开销较高但频率低。
4. **Python 独立可用**：所有 Python 模块可在不启动 Rust 程序的情况下独立运行，适合离线后处理和脚本化工作流。

### 4.2 两种 Python 调用模式

| 模式 | 触发方式 | 数据传递 | 适用场景 |
|------|----------|----------|----------|
| **子进程模式** | `pre_script` / `post_script` TOML 配置 | 通过文件（TOML、NPZ） | 重量级预处理、完整后处理流水线 |
| **FFI 模式** | `python-ffi` Cargo 特性 + `plot_interval` | 直接内存传递（无文件） | 低频云图、实时诊断 |

两种模式互不排斥，可以同时启用。

---

## 五、模块间调用关系

### 5.1 完整仿真流程

```
┌─────────────────────────────────────────────────────────────────────┐
│                         完整仿真流程                                 │
│                                                                     │
│  ① 可选：Python 子进程预处理                                         │
│     lbm_pre.config_gen  → 生成 configs/*.toml                       │
│     lbm_pre.mesh        → 生成 domain.msh                           │
│     通过命令行参数传递配置文件路径                                    │
│                          ↓                                          │
│  ② Rust 主控（orchestrator/src/main.rs）                             │
│     读取 configs/*.toml                                              │
│     [可选] Python FFI：调用 lbm_pre.bridge.geometry_markers_raw()    │
│       → 直接获取标记点坐标（无 CSV 文件）                             │
│     调用 C++ 核心（FFI）运行 LBM 仿真循环                             │
│       每隔 write_interval 步 → NPZ 快照（Rust 原生写出）              │
│       每步（可选）           → CSV 一行（Rust 原生追加）               │
│       每隔 plot_interval 步  → Python FFI 云图（无 NPZ 中间文件）     │
│                          ↓                                          │
│  ③ 可选：Python 子进程后处理                                         │
│     lbm_post.vtk_reader → 读取 output/fluid_*.npz                   │
│     lbm_post.plot       → 生成 PNG 图像                              │
│     lbm_post.analysis   → 计算阻力/升力、体积平均量等                 │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.2 Rust ↔ C++ 调用（FFI）

`bindings/src/lib.rs` 通过 C ABI 调用 C++ 核心库：

```
Rust orchestrator
    │
    ├─ LbmGrid::new(nx, ny, nz, model)
    │       └─→ C ABI: lbm_grid_new()  →  C++ LatticeGrid（堆分配）
    │
    ├─ LbmSolver::new(&mut grid, omega, cm)
    │       └─→ C ABI: lbm_solver_new() → C++ Solver（堆分配）
    │
    ├─ solver.step(&mut grid)             ← 每个时间步执行碰撞+迁移
    │       └─→ C ABI: lbm_solver_step()
    │
    └─ grid.rho(idx) / .ux(idx) / .uy(idx)
            └─→ C ABI: lbm_grid_rho/ux/uy() ← 读取格点流场数据
```

`bindings/build.rs` 在 Rust 编译期自动调用 cmake 构建 `liblbm_core.a`，并通过 `cargo:rustc-link-lib=static=lbm_core` 链接。

### 5.3 Rust → Python 子进程调用

Rust 主控使用 `std::process::Command` 以子进程方式调用 Python 脚本：

```
main() 主控
    │
    ├─ [可选] 预处理阶段
    │   run_python_subprocess(interpreter, pre_script, [config_path])
    │       └─→ python3 <pre_script> configs/my_run.toml
    │
    ├─ [仿真循环]
    │
    └─ [可选] 后处理阶段
        run_python_subprocess(interpreter, post_script, [output_dir])
            └─→ python3 <post_script> output/lid_cavity
```

**接口规范**：
- 预处理脚本接收一个位置参数：TOML 配置文件路径。
- 后处理脚本接收一个位置参数：输出目录路径。
- 脚本退出码非 0 时，Rust 主程序报错退出。

### 5.4 Rust → Python FFI 调用（pyo3）

启用 `python-ffi` Cargo 特性后，Rust 通过 pyo3 在进程内直接调用 Python：

```
main() [需要 --features python-ffi]
    │
    ├─ python_bridge::add_python_path("python/")
    │       └─→ pyo3: sys.path.insert(0, "python/")
    │
    ├─ python_bridge::markers_from_geometry("circle", 50.0, 40.0, 10.0, 64)
    │       └─→ pyo3: lbm_pre.bridge.geometry_markers_raw("circle", ...)
    │               → 返回 (Vec<f64>, Vec<f64>, Vec<f64>)  ← 无文件
    │
    └─ python_bridge::plot_field(&rho, &ux, &uy, nx, ny, step, time,
                                  out_dir, "velocity_magnitude")
            └─→ pyo3: lbm_post.bridge.plot_field_raw(rho_list, ux_list, uy_list, ...)
                    → numpy reshape → matplotlib 绘图 → 保存 PNG  ← 无 NPZ 中间文件
```

**数据传递方式**：Rust `&[f64]` 切片 → `pyo3::types::PyList` → Python 函数参数。NumPy 在 Python 侧负责 reshape（`np.asarray(rho).reshape(ny, nx)`），Rust 侧无需了解 NumPy 内部格式。

### 5.5 Python 独立使用路径（无 Rust）

Python 模块设计为可独立运行，无需 Rust 程序：

```
用户 Python 脚本
    │
    ├─ from lbm_pre.config_gen import SolverConfig
    │   SolverConfig.lid_driven_cavity().write("configs/my.toml")
    │
    ├─ from lbm_pre.geometry import circle_markers, write_markers_csv
    │   markers = circle_markers(50, 40, 10, 128)
    │   write_markers_csv(markers, "markers.csv")     ← 生成文件
    │
    ├─ from lbm_pre.bridge import geometry_markers_raw
    │   x, y, ds = geometry_markers_raw("circle", 50, 40, 10, 128)  ← 无文件
    │
    ├─ from lbm_post.vtk_reader import NpzReader
    │   snap = NpzReader("output/").last()
    │
    ├─ from lbm_post.plot import plot_velocity_magnitude, save_figure
    │   fig, _ = plot_velocity_magnitude(snap)
    │   save_figure(fig, "vel_mag.png")
    │
    └─ from lbm_post.bridge import plot_field_raw
        plot_field_raw(rho, ux, uy, nx, ny, step, time, "output/", "vorticity")
```

---

## 六、数据传输格式要求

### 6.1 TOML 配置文件（Python → Rust）

Python 端用 `lbm_pre.config_gen.SolverConfig.write()` 生成，Rust 端用 `Config::from_file()` 解析。

完整结构：

```toml
[simulation]
n_steps        = 10000          # 总时间步数
dt             = 1.0            # 时间步长（格子单位）
lattice_model  = "D2Q9"         # "D2Q9" | "D3Q19" | "D3Q27"
collision_model = "MRT"         # "BGK" | "MRT"

[fluid]
nx   = 100                      # x 方向格点数
ny   = 100                      # y 方向格点数
nz   = 1                        # z 方向格点数（2D 仿真设 1）
nu   = 0.01                     # 运动粘度（格子单位，建议 0.05 ~ 0.5）
rho0 = 1.0                      # 初始均匀密度

  [[fluid.boundary_conditions]]
  bc_type = "zou_he_velocity"   # "bounce_back" | "zou_he_velocity" | "zou_he_pressure"
  face    = "north"             # "west" | "east" | "south" | "north" | "bottom" | "top"
  ux      = 0.1                 # 边界 x 速度（jou_he_velocity 有效）
  uy      = 0.0
  rho     = 1.0                 # 边界密度（zou_he_pressure 有效）

[structure]                     # 可选，FSI 结构参数
young_modulus  = 1.0e3
second_moment  = 1.0e-6
density        = 1.5
area           = 0.01
length         = 0.3
n_elements     = 10

[ibm]                           # 可选，IBM 几何参数
geometry   = "circle"           # "circle" | "filament"
x0         = 50.0               # 圆心/起点 x（格子单位）
y0         = 40.0               # 圆心/起点 y
size       = 10.0               # 半径（circle）/ 长度（filament）
n_markers  = 64                 # 拉格朗日标记点数
delta_kernel = "four_point"     # "two_point" | "four_point"

[output]
write_interval     = 500        # NPZ 快照输出间隔（高频原生写出）
directory          = "output/lid_cavity"
plot_interval      = 2000       # Python FFI 云图间隔（可选，低频）
enable_csv_monitor = true       # 每步追加 CSV 一行（轻量时序输出）

[python]                        # 可选
interpreter = "python3"         # Python 可执行文件路径
pre_script  = "scripts/pre.py"  # 预处理脚本（子进程模式，可省略）
post_script = "scripts/post.py" # 后处理脚本（子进程模式，可省略）
pythonpath  = "python"          # 追加到 sys.path，使 lbm_pre/lbm_post 可导入
```

#### 参数约束

| 参数 | 类型 | 约束 |
|------|------|------|
| `nu` | float64 | 建议 0.05 ≤ ν ≤ 0.5（保证 τ ∈ (0.5, 2)） |
| `n_steps` | uint64 | ≥ 0 |
| `write_interval` | uint64 | ≥ 1 |
| `lattice_model` | string | "D2Q9" / "D3Q19" / "D3Q27" |
| `collision_model` | string | "BGK" / "MRT" |

### 6.2 NPZ 快照文件（Rust → Python）

Rust `output::write_snapshot_npz()` 每隔 `write_interval` 步写出。  
Python `lbm_post.vtk_reader.NpzReader` 读取。

文件名：`<output_dir>/fluid_<NNNNNN>.npz`  
格式：NumPy `.npz`（ZIP 压缩包，内含 `.npy` 文件）

| 数组键 | 形状 | 类型 | 说明 |
|--------|------|------|------|
| `rho` | `(ny, nx)` | float64 | 密度场，行主序 `[j, i]` 对应格点 `(i, j)` |
| `ux`  | `(ny, nx)` | float64 | x 方向速度场 |
| `uy`  | `(ny, nx)` | float64 | y 方向速度场 |
| `step` | 标量 | int64 | 时间步索引 |
| `time` | 标量 | float64 | 物理时间（= step × dt） |

**Python 读取示例：**
```python
from lbm_post.vtk_reader import NpzReader
snap = NpzReader("output/lid_cavity").last()
print(snap.rho.shape)    # (100, 100)
```

### 6.3 CSV 监控日志（Rust 写出，供任意工具读取）

当 `enable_csv_monitor = true` 时，Rust `output::append_monitor_csv()` 每步追加一行。

文件名：`<output_dir>/monitor.csv`

格式（CSV，无二进制依赖）：

```csv
step,time,ke
1,1.000000,5.1234567890e-4
2,2.000000,5.1234567890e-4
...
```

| 列 | 类型 | 说明 |
|----|------|------|
| `step` | int | 时间步索引 |
| `time` | float | 物理时间 |
| `ke` | float | 体积平均动能 0.5·mean(ux²+uy²) |

**适用场景**：高频时序监控（每步记录），文件极小，可直接用 Excel/Python/gnuplot 打开。

### 6.4 Python FFI 直接传递（pyo3，无中间文件）

#### 网格标记（Rust 接收来自 Python 的数据）

调用 `python_bridge::markers_from_geometry()` → `lbm_pre.bridge.geometry_markers_raw()`：

```
Rust 调用参数：(geometry: &str, x0, y0, size, n_markers)
Python 返回值：(x: list[float], y: list[float], ds: list[float])
Rust 接收类型：(Vec<f64>, Vec<f64>, Vec<f64>)
```

#### 云图绘制（Rust 发送数据给 Python）

调用 `python_bridge::plot_field()` → `lbm_post.bridge.plot_field_raw()`：

```
Rust 发送数据：
  rho: &[f64]   → PyList<f64>  (len = nx * ny，行主序)
  ux:  &[f64]   → PyList<f64>
  uy:  &[f64]   → PyList<f64>
  nx, ny: usize → int
  step: u64     → int
  time: f64     → float
  out_dir: &str → str
  field: &str   → str  ("velocity_magnitude" | "vorticity" | "pressure" | "streamlines")

Python 处理：np.asarray(rho).reshape(ny, nx) → FieldSnapshot → matplotlib → PNG
```

### 6.5 CSV 标记文件（Python 独立模式，可选）

当 Python 独立运行时，`lbm_pre.geometry.write_markers_csv()` 可将标记点写为 CSV：

```csv
# tag: circle
x,y,ds
50.0,60.0,0.9817477
...
```

| 列 | 类型 | 说明 |
|----|------|------|
| `x` | float | 标记点 x 坐标（格子单位） |
| `y` | float | 标记点 y 坐标（格子单位） |
| `ds` | float | 对应弧长元素（用于 IBM 积分） |

---

## 七、输出策略对比

| 策略 | 频率 | 数据量 | 实现 | 适用场景 |
|------|------|--------|------|----------|
| NPZ 快照 | 低（每数百/千步） | 全场（ny×nx×3×8 B） | Rust 原生（`output.rs`） | 全场数据存档，供离线后处理 |
| CSV 监控 | 高（每步） | 极小（1 行 ≈ 100 B） | Rust 原生追加 | 时序监控、收敛判断 |
| Python FFI 云图 | 最低（每数千步） | 无（直接生成 PNG） | pyo3 → matplotlib | 运行时可视化诊断 |
| Python 子进程 | 仅开始/结束 | 通过文件 | subprocess | 重量级预/后处理流水线 |

---

## 八、构建与运行

### 8.1 前置条件

- CMake ≥ 3.20
- C++17 编译器（GCC ≥ 11 或 Clang ≥ 14）
- MPI 实现（OpenMPI ≥ 4.0 或 MPICH ≥ 3.4）
- Rust 工具链 ≥ 1.70（`rustup`）
- Python ≥ 3.11 + pip
- （仅 `python-ffi` 特性）Python 开发头文件（`python3-dev`）

### 8.2 安装 Python 依赖

```bash
cd python
pip install -e ".[test]"    # 安装 numpy, matplotlib, scipy, pytest, gmsh
```

### 8.3 构建

```bash
# 标准构建（无 Python FFI）
cargo build --release

# 启用 Python FFI 构建（需要 python3-dev）
cargo build --release --features python-ffi
```

### 8.4 运行

```bash
# 基本运行（标准配置）
./target/release/lbm-ibm-fsi --config configs/lid_driven_cavity.toml

# 覆盖时间步数（快速测试）
./target/release/lbm-ibm-fsi --config configs/lid_driven_cavity.toml --steps 100

# 启用 Python FFI 云图（需要 python-ffi 特性构建）
# 在 TOML 中配置 [output] plot_interval = 1000 及 [python] pythonpath = "python"
./target/release/lbm-ibm-fsi --config configs/my_run.toml

# MPI 多进程
mpirun -n 4 ./target/release/lbm-ibm-fsi --config configs/lid_driven_cavity.toml
```

### 8.5 独立运行 Python 端到端示例

```bash
cd python
PYTHONPATH=. python3 examples/lid_driven_cavity.py
```

---

## 九、测试

```bash
# C++ 单元测试
cmake --build build --target tests && ctest --test-dir build

# Rust 单元测试（包括 NPZ 编码、CSV 写出、配置解析）
cargo test

# Rust 测试（含 python-ffi 特性）
cargo test --features python-ffi

# Python 测试（全部，包括新增 bridge 测试）
cd python && PYTHONPATH=. python3 -m pytest tests/ -v --ignore=tests/test_mesh.py
```

---

## 十、扩展指南

### 10.1 添加新的 Python FFI 函数

1. 在 `python/lbm_pre/bridge.py` 或 `python/lbm_post/bridge.py` 中添加接受 `list[float]` 参数的函数；
2. 在 `orchestrator/src/python_bridge.rs` 中添加对应的 Rust 函数（使用 `#[cfg(feature = "python-ffi")]` 保护）；
3. 在 `orchestrator/src/main.rs` 中在适当位置调用；
4. 在 `python/tests/test_bridge.py` 中添加测试。

### 10.2 添加新的原生输出格式

在 `orchestrator/src/output.rs` 中仿照 `write_snapshot_npz` 或 `append_monitor_csv` 添加新函数，然后在 `main.rs` 仿真循环中调用。

### 10.3 添加新的 C++ 物理模块

1. 在 `core/include/` 和 `core/src/` 中实现新模块；
2. 在 `core/src/capi/` 中添加 C ABI 接口函数；
3. 在 `bindings/src/lib.rs` 中添加对应的 Rust 安全包装；
4. 在 `orchestrator/src/main.rs` 中调用。

---

## 十一、插件扩展架构

本节说明如何利用内置的插件系统为框架添加以下四类扩展，而**无需修改任何已有代码**：

1. 新边界条件
2. 新网格划分/处理方式（含自适应网格）
3. 新流体动网格 / 固体结构运动模型
4. 新柔性结构求解器

---

### 11.1 设计原则

插件系统遵循**开闭原则（Open-Closed Principle）**：核心求解器对修改封闭，对扩展开放。

```
┌─────────────────────────────────────────────────────────────────┐
│                     插件系统分层架构                             │
│                                                                 │
│  C++ 接口层（纯虚类，headers-only）                              │
│  ┌──────────────────┐  ┌───────────────┐                       │
│  │ IBoundaryPlugin  │  │ IMeshPlugin   │                       │
│  │ IMotionPlugin    │  │ IFlexibleSolverPlugin                 │
│  └──────────────────┘  └───────────────┘                       │
│            ↑                    ↑                               │
│         子类化               子类化                              │
│  ┌──────────────────────────────────────────┐                  │
│  │           PluginRegistry (singleton)     │                  │
│  │  boundary_  mesh_  motion_  flexible_    │                  │
│  │  apply_boundary() adapt_mesh()  ...      │                  │
│  └──────────────────────────────────────────┘                  │
│            ↑                                                    │
│   C ABI 适配（plugin_registry.cpp）                             │
│   lbm_set_plugins(boundary_fn, mesh_fn, motion_fn, flex_fn)    │
│            ↑                                                    │
│   Rust 安全封装（bindings/src/lib.rs）                          │
│   register_plugins(PluginCallbacks { boundary_fn: Some(...) }) │
│            ↑                                                    │
│   TOML 配置（orchestrator/src/config.rs）                       │
│   [plugins]  boundary = "..."  mesh = "..."  ...               │
└─────────────────────────────────────────────────────────────────┘
```

**插件调用顺序**（在每个时间步内）：

```
IMotionPlugin::update()         ← 步骤前：更新位置
  → Solver::step()              ← 标准 LBM 碰撞 + 迁移
  → IBoundaryPlugin::apply()    ← 迁移后：自定义边界
  → IMeshPlugin::adapt()        ← 迁移后：网格自适应
  → IFlexibleSolverPlugin::step() ← 迁移后：柔性结构推进
```

---

### 11.2 扩展点一：新增边界条件（IBoundaryPlugin）

#### 接口文件
`core/include/plugins/boundary_plugin.hpp`

#### 使用场景
- 对流出口边界（Convective Outlet）
- 非反射边界条件（NRBC）
- 开放压力出口
- 自定义多孔介质壁面

#### C++ 实现步骤

```cpp
// my_bc_plugin.hpp
#include "plugins/boundary_plugin.hpp"
#include "lbm/lattice.hpp"

class ConvectiveOutlet : public lbm::IBoundaryPlugin {
public:
    void apply(lbm::LatticeGrid& grid, int step) override {
        // 在 East 面施加对流出口 BC
        // grid.f 包含迁移后的分布函数，直接修改即可
        const int nx = grid.nx;
        const int ny = grid.ny;
        for (int j = 0; j < ny; ++j) {
            int n = grid.idx(nx - 1, j);
            // ... 对流出口公式 ...
        }
    }
    const char* name() const override { return "convective_outlet"; }
};
```

#### 注册方式（C++ 侧）

```cpp
#include "plugins/plugin_registry.hpp"
ConvectiveOutlet my_bc;
lbm::PluginRegistry::instance().set_boundary_plugin(&my_bc);
```

#### 注册方式（Rust 侧）

```rust
use lbm_bindings::{PluginCallbacks, register_plugins};

unsafe extern "C" fn my_bc_fn(
    grid: *mut std::ffi::c_void,
    step: i32,
    _data: *mut std::ffi::c_void,
) {
    // 将 grid 转型后应用自定义 BC
    let _ = (grid, step);
}

let mut cbs = PluginCallbacks::default();
cbs.boundary_fn = Some(my_bc_fn);
register_plugins(cbs);
```

#### TOML 配置

```toml
[plugins]
boundary = "convective_outlet"  # 仅作记录日志用，不自动加载共享库
```

---

### 11.3 扩展点二：新增网格划分处理方式（IMeshPlugin）

#### 接口文件
`core/include/plugins/mesh_plugin.hpp`

#### 使用场景
- 自适应均匀加密（局部细化）
- z 方向拉伸格子（非均匀间距）
- Overset / Chimera 嵌套网格
- 区域分解重分配（负载均衡）

#### C++ 实现步骤

```cpp
#include "plugins/mesh_plugin.hpp"

class AdaptiveRefinement : public lbm::IMeshPlugin {
public:
    void initialize(lbm::LatticeGrid& grid,
                    const void* cfg) override {
        // 读取 cfg 中的细化参数，初始化辅助数据结构
    }

    void adapt(lbm::LatticeGrid& grid, int step) override {
        if (step % refine_interval_ == 0) {
            // 1. 计算误差指示器（涡量梯度等）
            // 2. 标记需要细化/粗化的格点
            // 3. 重新分配 grid.f、grid.rho、grid.u 数组
        }
    }

    const char* name() const override { return "adaptive_uniform"; }

private:
    int refine_interval_ = 500;
};
```

#### 关键数据访问

- `grid.f[node * q + direction]` — 分布函数（可直接修改）
- `grid.rho[node]`、`grid.u[node * dim + d]` — 宏观量
- 网格尺寸通过 `grid.nx / ny / nz` 访问；调整尺寸需同步修改 `grid.f.resize()`

---

### 11.4 扩展点三：新增流体动网格 / 固体结构运动（IMotionPlugin）

#### 接口文件
`core/include/plugins/motion_plugin.hpp`

#### 使用场景
- 旋转圆柱（prescribed sine/cosine 运动）
- 直升机旋翼（Chimera + 刚体 6-DOF）
- 任意拉格朗日-欧拉（ALE）滑移面
- 柔性叶片的刚体整体平移

#### 方法签名

```cpp
void update(lbm::LatticeGrid& grid,
            ibm::MarkerSet* markers,   // 可为 nullptr（纯 LBM 无 IBM 时）
            double dt,
            int step) override;
```

#### C++ 实现骨架

```cpp
#include "plugins/motion_plugin.hpp"
#include "ibm/marker.hpp"
#include <cmath>

class PrescribedSineMotion : public lbm::IMotionPlugin {
public:
    void update(lbm::LatticeGrid& /*grid*/,
                ibm::MarkerSet* markers,
                double dt, int step) override
    {
        if (!markers) return;
        const double t = step * dt;
        const double dy = amplitude_ * std::sin(2.0 * M_PI * freq_ * t) * dt;
        for (auto& m : markers->markers) {
            m.y += dy;
            m.y0 = m.y;   // 更新参考位置（用于 IBM 力计算）
        }
    }
    const char* name() const override { return "prescribed_sine"; }
private:
    double amplitude_ = 2.0;
    double freq_      = 0.05;
};
```

#### 注意事项
- `markers` 在无 IBM 场景下为 `nullptr`，**必须先判空**。
- 移动欧拉格点（ALE）时需同步更新 `grid.u` 中的网格速度，以便在 Guo 外力修正中正确处理相对速度。

---

### 11.5 扩展点四：新增柔性结构求解器（IFlexibleSolverPlugin）

#### 接口文件
`core/include/plugins/flexible_plugin.hpp`

#### 使用场景
- Kirchhoff-Love 薄板（替代 Euler-Bernoulli 梁）
- 共旋转（Co-rotational）大变形梁
- 有限体积膜（3-D 柔性表面）
- 简化降阶模型（POD-ROM 快速代理）
- 外部 FEM 程序耦合（如 Calculix、Code_Aster）

#### 方法签名

```cpp
void step(ibm::MarkerSet& markers, double dt, int step_idx) override;
```

**入口状态**：`markers.markers[i].{ux, uy}` 已由速度插值填充（流体 → 结构的速度耦合）。  
**出口要求**：`markers.markers[i].{x, y, fx, fy}` 必须更新（新位置和 IBM 力密度），以供后续 `spread_force()` 使用。

#### C++ 实现骨架

```cpp
#include "plugins/flexible_plugin.hpp"

class KirchhoffPlate : public lbm::IFlexibleSolverPlugin {
public:
    void step(ibm::MarkerSet& markers, double dt, int step_idx) override {
        // 1. 从 markers 提取节点力
        for (const auto& m : markers.markers) {
            append_nodal_force(m.fx, m.fy);
        }
        // 2. 求解板方程  M*a + K*u = F
        solve_plate(dt);
        // 3. 将新挠度写回 markers 的 x, y 坐标
        int i = 0;
        for (auto& m : markers.markers) {
            m.x  = new_x_[i];
            m.y  = new_y_[i];
            m.fx = restoring_fx_[i];  // IBM 直接力
            m.fy = restoring_fy_[i];
            ++i;
        }
    }
    const char* name() const override { return "kirchhoff_love_plate"; }
private:
    void append_nodal_force(double fx, double fy) { /* ... */ }
    void solve_plate(double dt) { /* FEM assembly + solve */ }
    std::vector<double> new_x_, new_y_;
    std::vector<double> restoring_fx_, restoring_fy_;
};
```

---

### 11.6 插件注册流程总结

#### 三种注册路径

| 路径 | 适合用户 | 代码位置 |
|------|----------|----------|
| C++ 虚函数子类化 | C++ 开发者 | `core/include/plugins/` |
| C ABI 函数指针 | C / Fortran 用户 | `lbm_set_plugins()` in `lbm_capi.cpp` |
| Rust 安全封装 | Rust 用户 | `register_plugins()` in `bindings/src/lib.rs` |

#### 完整 Rust 使用示例

```rust
use lbm_bindings::{LatticeModel, CollisionModel, LbmGrid, LbmSolver,
                   PluginCallbacks, register_plugins};

// 定义自定义边界条件回调（C ABI 兼容）
unsafe extern "C" fn my_open_bc(
    grid_ptr: *mut std::ffi::c_void,
    step: i32,
    _data: *mut std::ffi::c_void,
) {
    // 将 grid_ptr 转型为 *mut lbm::LatticeGrid 并应用 BC
    // （需要 unsafe + C++ 布局知识，建议封装为 C++ 子类后通过 C ABI 调用）
    let _ = (grid_ptr, step);
}

// 在仿真循环前注册
let mut cbs = PluginCallbacks::default();
cbs.boundary_fn = Some(my_open_bc);
register_plugins(cbs);  // 写入全局 PluginRegistry 单例

// 此后每次 solver.step() 内部会自动调用 my_open_bc
let mut grid = LbmGrid::new(100, 100, 1, LatticeModel::D2Q9);
let mut solver = LbmSolver::new(&mut grid, 1.0, CollisionModel::Bgk);
for step in 0..1000 {
    // step_n() 将 step_index 和 dt 传递给插件，使插件能获取精确时间信息
    solver.step_n(&mut grid, step, 1.0);
}
```

#### TOML 配置示例（完整）

```toml
[simulation]
n_steps = 10000
dt = 1.0

[fluid]
nx = 200
ny = 100
nu = 0.02

[ibm]
geometry  = "circle"
x0        = 60.0
y0        = 50.0
size      = 10.0
n_markers = 128

[output]
write_interval     = 500
enable_csv_monitor = true

[plugins]
# 插件名称仅用于启动日志打印；实际插件逻辑必须在 Rust/C++ 代码中注册。
boundary = "convective_outlet"     # IBoundaryPlugin
mesh     = ""                      # 不使用自适应网格
motion   = "prescribed_sine"       # IMotionPlugin（正弦驱动运动）
flexible = "kirchhoff_love_plate"  # IFlexibleSolverPlugin（薄板替换梁求解器）
```

---

### 11.7 添加新插件的检查清单

新增任意插件时，按以下顺序操作：

- [ ] **接口定义**：选择对应的 `I*Plugin` 抽象类（在 `core/include/plugins/` 中），继承并实现纯虚函数。
- [ ] **注册**：在仿真主循环前调用 `PluginRegistry::instance().set_*_plugin(&my_plugin)` 或 `register_plugins(cbs)`。
- [ ] **TOML 记录**（可选）：在 `[plugins]` 节中填写插件名称，便于日志追溯。
- [ ] **测试**：在 `core/tests/` 下添加 `test_plugin_*.cpp`，或在 Rust 侧添加单元测试，验证插件在 0 步、1 步、多步情况下的行为。
- [ ] **文档**：在本节新增插件描述，说明入口/出口数据要求。

---

## 十二、编译与运行详解

本章完整解释 `cargo build` 触发的全部编译链路，以及各个 CMakeLists.txt 文件在构建过程中的作用和执行顺序。

---

### 12.1 快速上手（一步到位）

```bash
# 1. 安装 Rust（若尚未安装）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source "$HOME/.cargo/env"

# 2. 安装 C++ 构建工具（Ubuntu / Debian 示例）
sudo apt-get install -y cmake g++ libopenmpi-dev libomp-dev

# 3. 克隆并进入项目
git clone https://github.com/kyoheyo/lbm-ibm-fsi.git
cd lbm-ibm-fsi

# 4. 构建（Rust + C++，默认 Debug 模式）
cargo build

# 5. 运行内置 lid-driven cavity 算例
./target/debug/lbm-ibm-fsi --config configs/lid_driven_cavity.toml --steps 100
```

Release（优化）构建：

```bash
cargo build --release
./target/release/lbm-ibm-fsi --config configs/lid_driven_cavity.toml
```

---

### 12.2 `cargo build` 执行逻辑全流程

当在项目根目录执行 `cargo build` 时，Cargo 按下图顺序处理**工作空间（workspace）**中的每个 crate：

```
cargo build
    │
    ├─ 读取  Cargo.toml（workspace）
    │         members = ["orchestrator", "bindings"]
    │
    ├─① 编译 lbm-bindings crate（bindings/）
    │   │
    │   ├─ 检测到 [package] links = "lbm_core"
    │   │  → 必须执行 bindings/build.rs（构建脚本）
    │   │
    │   ├─② 运行 bindings/build.rs
    │   │   │
    │   │   ├─ cmake::Config::new(repo_root)        ← 指定顶层 CMakeLists.txt 所在目录
    │   │   │   .define("BUILD_TESTS", "OFF")
    │   │   │   .define("ENABLE_MPI",    "OFF")     ← 默认关闭，见 §12.4
    │   │   │   .define("ENABLE_OPENMP", "OFF")
    │   │   │   .build()                            ← 触发 cmake configure + make
    │   │   │
    │   │   │   cmake 执行顺序（见 §12.3）：
    │   │   │     ③ CMakeLists.txt（顶层）          ← 全局选项、编译器配置
    │   │   │     ④ core/CMakeLists.txt             ← 编译 lbm_core.a
    │   │   │     （core/tests/CMakeLists.txt 因 BUILD_TESTS=OFF 被跳过）
    │   │   │
    │   │   ├─ println!("cargo:rustc-link-search=native=.../lib")
    │   │   │   → 告诉 rustc: 在该目录寻找 .a 文件
    │   │   │
    │   │   ├─ println!("cargo:rustc-link-lib=static=lbm_core")
    │   │   │   → 告诉 rustc: 链接 liblbm_core.a
    │   │   │
    │   │   └─ println!("cargo:rustc-link-lib=stdc++")
    │   │       → 链接 C++ 标准库（macOS 上改为 c++）
    │   │
    │   └─ 编译 bindings/src/lib.rs
    │       → 生成 liblbm_bindings.rlib
    │
    └─③ 编译 lbm-ibm-fsi crate（orchestrator/）
        │  依赖 lbm-bindings（已在上一步编译完成）
        │
        ├─ 编译 orchestrator/src/config.rs
        ├─ 编译 orchestrator/src/output.rs
        ├─ 编译 orchestrator/src/python_bridge.rs
        └─ 编译 orchestrator/src/main.rs
            → 最终链接：lbm_ibm_fsi + lbm_bindings + liblbm_core.a + libstdc++
            → 生成可执行文件 target/debug/lbm-ibm-fsi
```

> **关键机制**：`[package] links = "lbm_core"` 告诉 Cargo 该 crate 负责链接名为 `lbm_core` 的原生库，整个 workspace 中只允许一个 crate 声明链接同一个原生库名称，从而避免重复链接冲突。

---

### 12.3 `build.rs` 逐行解析

文件位置：`bindings/build.rs`

```rust
fn main() {
    // ① 获取 bindings/ 目录的绝对路径
    let manifest_dir = PathBuf::from(env!("CARGO_MANIFEST_DIR"));
    // ② 向上一级 = 项目根目录（包含顶层 CMakeLists.txt）
    let repo_root = manifest_dir.parent().unwrap();

    // ③ 调用 cmake crate 驱动 CMake 构建
    //    cmake::Config::new(src_dir) 指定 CMake 源码树根目录
    let dst = cmake::Config::new(repo_root)
        // 关闭 C++ 测试构建，加速 Rust 构建链路
        .define("BUILD_TESTS", "OFF")
        // MPI/OpenMP：Rust 侧默认关闭（避免 CI 环境依赖问题）；
        // 若需要，设置环境变量 LBM_ENABLE_MPI=ON / LBM_ENABLE_OPENMP=ON 后重新构建
        .define("ENABLE_MPI",    std::env::var("LBM_ENABLE_MPI")
                                    .unwrap_or_else(|_| "OFF".into()))
        .define("ENABLE_OPENMP", std::env::var("LBM_ENABLE_OPENMP")
                                    .unwrap_or_else(|_| "OFF".into()))
        // .build() 等价于：
        //   cmake <repo_root> -DBUILD_TESTS=OFF ... -B <out_dir>/build
        //   cmake --build <out_dir>/build
        // 返回值 dst = CMake install prefix（Cargo 管理的临时目录）
        .build();

    // ④ 告知 rustc 在 cmake install prefix 下的 lib/ 目录中查找链接库
    println!("cargo:rustc-link-search=native={}/lib", dst.display());

    // ⑤ 静态链接 liblbm_core.a
    println!("cargo:rustc-link-lib=static=lbm_core");

    // ⑥ 链接 C++ 标准库（平台判断）
    #[cfg(target_os = "macos")]
    println!("cargo:rustc-link-lib=c++");
    #[cfg(not(target_os = "macos"))]
    println!("cargo:rustc-link-lib=stdc++");

    // ⑦ 增量构建守卫：以下路径发生变化时重新执行 build.rs
    println!("cargo:rerun-if-changed=../core/src");
    println!("cargo:rerun-if-changed=../core/include");
    println!("cargo:rerun-if-env-changed=LBM_ENABLE_MPI");
    println!("cargo:rerun-if-env-changed=LBM_ENABLE_OPENMP");
}
```

**`cargo:rerun-if-changed` 机制**：Cargo 在每次构建时比较文件修改时间。只要 `core/src/` 或 `core/include/` 下有文件发生变动，就会重新运行 `build.rs`，从而触发增量 CMake 重新编译。没有变化时 `lbm_core.a` 直接从缓存中读取，无需重新编译 C++ 代码。

---

### 12.4 各 CMakeLists.txt 执行顺序与作用

CMake 构建按如下顺序处理三个 CMakeLists.txt 文件：

---

#### 第 1 步：`CMakeLists.txt`（顶层，项目根目录）

```
作用：全局编译环境配置
触发时机：cmake::Config::new(repo_root).build() 首先处理此文件
```

| 指令 | 说明 |
|------|------|
| `cmake_minimum_required(VERSION 3.20)` | 检查 CMake 版本，低于 3.20 立即报错 |
| `project(lbm_ibm_fsi ...)` | 声明项目名称、版本、使用的语言（CXX） |
| `set(CMAKE_CXX_STANDARD 17)` | 强制所有子目标使用 C++17 |
| `option(ENABLE_MPI ...)` | 定义 `ENABLE_MPI` 开关，默认 ON；`build.rs` 传入 `OFF` 以禁用 |
| `option(ENABLE_OPENMP ...)` | 同上，OpenMP 并行开关 |
| `option(ENABLE_CUDA ...)` | CUDA GPU 加速开关，默认 OFF |
| `option(BUILD_TESTS ...)` | 是否编译 C++ 单元测试，`build.rs` 传入 `OFF` |
| `add_compile_options(-O3 -march=native ...)` | GCC/Clang 下启用 O3 优化和本机指令集 |
| `find_package(MPI REQUIRED)` | 若 `ENABLE_MPI=ON`，查找系统 MPI 并报错退出（若未找到） |
| `find_package(OpenMP REQUIRED)` | 若 `ENABLE_OPENMP=ON`，查找 OpenMP 并报错退出 |
| `add_subdirectory(core)` | **递归进入 `core/CMakeLists.txt`（第 2 步）** |
| `add_subdirectory(core/tests)` | 若 `BUILD_TESTS=ON` 才递归进入测试 CMakeLists.txt（第 3 步，Rust 构建时跳过） |

---

#### 第 2 步：`core/CMakeLists.txt`（核心库）

```
作用：编译所有 C++ 计算代码，生成静态库 liblbm_core.a
触发时机：顶层 add_subdirectory(core) 触发
```

| 指令 | 说明 |
|------|------|
| `set(CORE_SOURCES ...)` | 列出需要编译的 9 个 `.cpp` 源文件（LBM / IBM / FSI / CAPI / Plugin） |
| `add_library(lbm_core STATIC ...)` | 将上述源文件编译并打包为静态库 `liblbm_core.a` |
| `target_include_directories(lbm_core PUBLIC ...)` | 向所有依赖 `lbm_core` 的目标暴露 `core/include/` 头文件路径 |
| `target_link_libraries(... MPI::MPI_CXX)` | 若 `ENABLE_MPI=ON`，链接 MPI 库并定义 `LBM_ENABLE_MPI` 宏 |
| `target_link_libraries(... OpenMP::OpenMP_CXX)` | 若 `ENABLE_OPENMP=ON`，链接 OpenMP 并定义 `LBM_ENABLE_OPENMP` 宏 |
| `set_target_properties(... ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/lib")` | 将生成的 `.a` 文件放入构建目录的 `lib/` 子目录（`build.rs` 中的 `rustc-link-search` 指向此处） |
| `install(TARGETS lbm_core ...)` | 定义 `cmake --install` 时库文件的安装目标（供独立安装使用） |

编译完成后，`build.rs` 接收到的 `dst` 路径下会有：

```
<Cargo临时构建目录>/
└── lib/
    └── liblbm_core.a   ← rustc 最终链接此文件
```

---

#### 第 3 步：`core/tests/CMakeLists.txt`（C++ 单元测试，Rust 构建时跳过）

```
作用：为 C++ 单元测试构建可执行文件
触发时机：仅当 BUILD_TESTS=ON 时，顶层 add_subdirectory(core/tests) 触发
         → Rust cargo build 时 BUILD_TESTS=OFF，此文件不被处理
         → 独立执行 cmake --build build 时默认执行
```

| 指令 | 说明 |
|------|------|
| `add_executable(test_lbm ...)` | 将 4 个测试源文件编译为可执行程序 `test_lbm` |
| `target_link_libraries(test_lbm PRIVATE lbm_core)` | 链接 `lbm_core` 库（自动继承其 include 路径） |
| `add_test(NAME lbm_tests COMMAND test_lbm)` | 向 CTest 注册测试，可通过 `ctest --test-dir build` 运行 |

---

### 12.5 编译产物说明

执行 `cargo build` 后各目录下的产物：

```
target/
├── debug/                         ← Debug 构建输出（cargo build）
│   ├── lbm-ibm-fsi                ← 最终可执行程序
│   ├── liblbm_bindings.rlib       ← Rust bindings crate 编译产物
│   └── build/
│       └── lbm-bindings-*/        ← build.rs 工作目录
│           └── out/
│               ├── build/         ← CMake 构建目录（cmake -B）
│               └── lib/
│                   └── liblbm_core.a  ← C++ 静态库（cmake 产物）
│
└── release/                       ← Release 构建输出（cargo build --release）
    └── lbm-ibm-fsi
```

---

### 12.6 环境变量与可选特性

| 变量 / 特性 | 说明 | 默认值 |
|-------------|------|--------|
| `LBM_ENABLE_MPI=ON` | `build.rs` 读取后传给 CMake；启用 MPI 并行（需 OpenMPI / MPICH） | OFF |
| `LBM_ENABLE_OPENMP=ON` | 启用 OpenMP 多线程并行 | OFF |
| `--features python-ffi` | 启用 pyo3 Python FFI 内联绘图，需 `python3-dev` | 未启用 |
| `CARGO_PROFILE_RELEASE_OPT_LEVEL=3` | 标准 release 已设为 3，通常无需手动设置 | 3 |

使用示例：

```bash
# 启用 MPI + OpenMP（C++ 侧并行）
LBM_ENABLE_MPI=ON LBM_ENABLE_OPENMP=ON cargo build --release

# 启用 Python FFI（Rust 嵌入 Python 解释器）
cargo build --release --features python-ffi

# 同时启用所有选项
LBM_ENABLE_MPI=ON LBM_ENABLE_OPENMP=ON \
  cargo build --release --features python-ffi
```

---

### 12.7 独立 C++ 构建（不经过 Rust）

如需单独编译 C++ 核心并运行 C++ 单元测试：

```bash
# 在项目根目录创建构建目录
cmake -S . -B build \
  -DCMAKE_BUILD_TYPE=Release \
  -DENABLE_MPI=ON \
  -DENABLE_OPENMP=ON \
  -DBUILD_TESTS=ON

# 编译
cmake --build build --parallel 4

# 运行 C++ 单元测试
ctest --test-dir build --output-on-failure
```

独立 C++ 构建与 `cargo build` 的区别：

| 对比项 | `cargo build` | 独立 cmake |
|--------|--------------|------------|
| 触发方式 | Rust 构建系统自动调用 | 手动执行 cmake 命令 |
| `BUILD_TESTS` | 默认 OFF（加速构建） | 默认 ON（会编译测试） |
| `ENABLE_MPI` | 默认 OFF（读取环境变量） | 默认 ON（CMakeLists.txt 设置） |
| `ENABLE_OPENMP` | 同上 | 默认 ON |
| 最终产物 | `target/*/lbm-ibm-fsi`（完整可执行程序） | `build/core/tests/test_lbm`（仅 C++ 测试） |

---

### 12.8 Cargo 缓存与增量编译原理

Cargo 通过以下机制决定是否重新编译：

1. **Rust 源文件变化**：Cargo 对每个 `.rs` 文件生成内容哈希；若哈希不变则不重编。

2. **`build.rs` 重运行判断**：`build.rs` 通过以下语句声明依赖：
   ```rust
   println!("cargo:rerun-if-changed=../core/src");
   println!("cargo:rerun-if-changed=../core/include");
   println!("cargo:rerun-if-env-changed=LBM_ENABLE_MPI");
   ```
   Cargo 记录这些路径的最后修改时间戳。只要 `core/src/` 或 `core/include/` 下有文件变动，或环境变量 `LBM_ENABLE_MPI` / `LBM_ENABLE_OPENMP` 发生变化，才重新运行 `build.rs`（进而触发增量 CMake 编译）。

3. **CMake 增量编译**：CMake 自身记录每个 `.cpp` 文件的依赖关系（通过 `-MD` 编译标志生成 `.d` 文件）。只有被修改的 `.cpp` 及其依赖的头文件对应的目标文件会被重新编译，最后重新打包成 `liblbm_core.a`。

4. **链接阶段**：只有 `liblbm_core.a` 或任意 `.rlib` 发生变化时，最终链接步骤才会重新执行。

典型增量构建时间（修改一个 `.cpp` 文件后）：

```
重新编译该 .cpp → 更新 liblbm_core.a → 重新链接 lbm-ibm-fsi
约 5–15 秒（取决于文件大小和机器性能）
```

---

### 12.9 常见问题排查

| 症状 | 可能原因 | 解决方法 |
|------|----------|----------|
| `cmake: command not found` | CMake 未安装 | `sudo apt-get install cmake` |
| `MPI not found` | OpenMPI/MPICH 缺失 | `sudo apt-get install libopenmpi-dev` 或设置 `LBM_ENABLE_MPI=OFF` |
| `OpenMP not found` | 编译器不支持 OpenMP | `sudo apt-get install libomp-dev` 或设置 `LBM_ENABLE_OPENMP=OFF` |
| `pyo3: Python interpreter not found` | 缺少 Python 开发头文件 | `sudo apt-get install python3-dev` |
| `liblbm_core.a: No such file` | cmake 构建失败 | 查看 `target/*/build/lbm-bindings-*/stderr` 中的 cmake 错误输出 |
| C++ 代码修改后未重编 | `rerun-if-changed` 路径不覆盖新文件 | 执行 `cargo clean` 后重新构建 |
| `cargo build` 很慢（首次） | 首次下载 crate + 编译所有 C++ | 预期 2–5 分钟；后续增量构建 < 30 秒 |
